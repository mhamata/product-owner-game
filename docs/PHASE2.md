# Phase 2+ — Sim 2.0 & learning-layer build ledger

*Cold-start state for the "complete the app" engagement (authorized by Mike 2026-07-12: "Complete the rest of the app the entire phases with all your recommendations start to finish"). Design authority: [design/design-sim-2.0.md](design/design-sim-2.0.md) + the two approved mockups beside it. This ledger tracks the build slices; update it every wave.*

**Scope ruling:** this engagement builds every web-side recommendation. It does NOT build the Capacitor iOS shell (gated on Phase 1 wedge results per MASTER_PLAN §9 — unchanged), and cannot run anything requiring `ANTHROPIC_API_KEY`/`SUPABASE_SERVICE_ROLE_KEY` live (Mike-gated; all AI surfaces ship with the standard calm degrade). Cred/economy is deliberately deferred until the loop proves itself — the mockups show it; the build omits it (open question #6).

## Slices

| Slice | What | State |
|---|---|---|
| W1-A | Engine people roster v1: named people (trust, mood, agenda, memory) deterministic from scenario+seed; trust effects through the event system. Engine-only. | 🔨 building |
| W1-B | App-wide light/dark theme tokens (design doc §3 table) + `/standup` 3-act home (due cards → scheduler-picked block → sim cliffhanger). | 🔨 building |
| W2-C | Engine board confidence + expectations + fired fail-state + end-of-season summary + deterministic job-market offers. Engine-only. | ✅ `engine/board.ts`: confidence (deterministic start, bounded per-sprint deltas, `board-confidence` EventEffect), 3 expectations, `fired` phase (floor 30, reachable only from review), `deriveSeasonSummary`/`deriveJobMarketOffers` selectors. 34 new tests, all gates green. |
| W2-D | Sim presentation rebuild: inbox turn (messages from people, decision sheets w/ telegraphed effects, rationale, commit→cliffhanger). | ✅ `InboxTurn.tsx` is now the sim experience at `/play/[scenarioId]`: Plan/event messages from `state.people` (trust bars, role labels), bottom-sheet decisions reusing `telegraphImpacts`/`deriveEventBeats`/(moved+exported) `summarizeEventEffects`, one-line rationale on the Commit sheet (same `decisionLogStore` capture as PR #10), cliffhanger with a "peek now" escape hatch, `OutcomeStep`/`DebriefStep` reused (lightly restyled, `hideStepBadge`) for the post-cliffhanger/next-visit review. `PreviewStep`/`ShipStep`/`EventStep`/`SimStepper`/`SimContextRail`/`SimScoreboard`/`PredictionCard`/`steps.ts` marked superseded (kept, not deleted). Handles W2-C's concurrently-landed `fired` phase with a placeholder (full UI is W3-F) and its `board-confidence` EventEffect in the effect chips. 15 new tests (explain.test.ts pure-logic + InboxTurn.test.tsx render/integration via the real engine reducer); no pre-existing render tests existed to adapt. All gates green (typecheck, 744/744 tests, lint at the 8-problem baseline, build, both leak greps empty). |
| W3-E | Product map screen (districts, health, debt hatching) + metric tiles with decision annotations. | ✅ Three-tab shell (`SimTabs.tsx`, Standup/Product/Season) now sits between `SimRunner` and `InboxTurn`; InboxTurn renders unmodified apart from one additive `commitBarBottomInset` prop so its fixed Commit bar clears the new tab bar (zero behavior changes — InboxTurn.test.tsx untouched and still green). `ProductMapScreen.tsx`: 4 metric tiles (revenue, team morale, tech debt, reliability) with inline SVG sparklines + amber decision-annotation dots (tap → rationale), a district map derived from `productId`-grouped PBIs (health/debt/hatching/dim, tap → detail sheet with a debt-attribution line), and one hardcoded-locked fog-of-war panel (`TODO(W4-G)`). New `productMap.ts` (pure derivation, documents exactly what's derivable: no scenario groups PBIs under a shared `productId` today, so a district is one PBI's group generically-keyed for when one does; "built" mirrors `step.ts`'s own `derivePriorDone`; discovery-pool items never show as a district until actually revealed — no spoilers) and `metricsHistoryStore.ts` (new zustand+persist store recording real per-sprint snapshots as the run plays, since GameState carries no metric history array — documents that `tech.reliability` is static in every scenario file, so its sparkline is always flat by design, not a bug). Both themes verified live (dev server + browser). 40 new unit tests (productMap + metricsHistoryStore pure helpers) + a SimTabs render test (tabs switch, InboxTurn still mounts, districts flip from "not built" once a sprint resolves). All gates green (typecheck, 786/786 tests, lint at the 8-problem baseline, build, both leak greps empty). |
| W3-F | Season screen: timeline, board meter, roster, QBR, fired→offers flow. | ✅ `SeasonScreen.tsx` replaces SimTabs.tsx's placeholder wholesale: sprint timeline (past/now/future, final sprint flagged QBR), board-confidence gradient meter + "fired below `FIRING_FLOOR`" honesty line + the 3 expectations with status pills, a 5-card roster (name/roleLabel/trust bar/agenda/most-recent "Remembers Sprint N: ..." memory line), a Career File summary card (counts derived only from `decisionLogStore` for THIS run — sprints logged, rationale count, event-response count, interview stories drafted — explicitly not the mockup's "graded artifacts"/"incident record" rows, which read from stores out of this slice's scope; "View full Career File → /report" links to PR #10's existing page), and a job-market card that's locked while live and reveals `deriveJobMarketOffers` as offer cards (company/level arrow+label/pitch) once `phase` is 'complete' or 'fired'. All of it consumes W2-C's `board.ts` selectors (`ensureBoard`, `deriveSeasonSummary`, `deriveJobMarketOffers`, `FIRING_FLOOR`) and W1-A's `people.ts` (`ensurePeopleRoster`, `roleLabel`) — nothing recomputed. New pure module `season.ts` (timeline model, job-market visibility, offer-card display meta, fired-surface routing, the fired beat's "record still proves" line, verdict copy, the Career File counts). FIRED experience: `SimTabs.tsx` now intercepts `phase === 'fired'` BEFORE any tab mounts and renders `FiredBeat.tsx` full-screen — the fact, the sprint, one derived proof line, "See your offers" (dismisses the beat, lands on Season with the job market already open) and "View your Career File" (→ /report); InboxTurn.tsx's W2-D fired placeholder is kept only as a defensive fallback for a direct caller (unreachable through the live SimTabs flow now) and was lightly re-worded to route the same two places. QBR: when `phase === 'complete'`, the Season tab leads with a QBR card reusing `deriveSeasonSummary`'s verdict + score (DebriefStep's own `DIMENSIONS`, no recompute) above the now-open job market — `TODO(W5-J)` marks where the AI multi-party QBR meeting slots in. **Known gap, not fixed by this slice**: `SimRunner.tsx` still special-cases `phase === 'complete'` straight to the pre-existing `SimEndPanel` (retro/difficulty-tier/evidence recording) *before* `SimTabs` ever mounts, so the QBR card is real and tested (`SimTabs.test.tsx` renders it directly) but is not yet reachable by a player finishing a live run through `SimRunner`'s current routing — wiring that hand-off is future-slice scope, left untouched here per the "exactly this slice" instruction (SimRunner.tsx wasn't in this slice's file list and touching it risks the retro/difficulty/evidence side effects that live there). Verified live in the browser (dev server), both themes, including the fired→offers flow via a real localStorage-forced `phase` transition — screenshots not committed. 30 new tests (`season.ts` pure derivations + `SimTabs.test.tsx` additions: live Season render, fired beat → dismiss → offers-open, complete → QBR + offers). All gates green (typecheck; 803/803 tests; lint at the 8-problem baseline; build; both leak greps — `SUPABASE_SERVICE_ROLE_KEY` empty, `ANTHROPIC_API_KEY` matches only pre-existing calm-degrade UI copy in untouched files like `SimEndPanel.tsx`/`EndGamePanel.tsx`, confirmed via source grep, not introduced by this slice). |
| W4-G | Fog-of-war: dashboard panes gated on `isMastered()`, unlock hints name the skill. | ✅ New pure registry `app/src/lib/fogOfWar.ts`: two real gates. `cohort-curves` unlocks with `reading-results` ("Significance & Cohorts", PM level · Experimentation unit) — the only curriculum skill whose own title names cohorts, replacing the W3-E placeholder's guess (`activation-retention`). `decision-annotations-history` unlocks with `metrics-literacy` (Foundations · Literacy) — the entry-level "read a metric" skill, a deliberately lower bar since that pane reads the player's own logged rationale, not third-party analytics. Both resolve to one real skill each; the design doc's competency-threshold fallback wasn't needed by either, so it's documented but not implemented (no dead/untested branch). `isFogGateUnlocked(gate, isMasteredSkill)` is a pure predicate pass-through — permanence comes from wiring it straight to `useLearnStore().isMastered()`, which is already monotonic (`mastery` is a running max, never decremented) and which W4-H's concurrently-landed `masteryDecay.ts` explicitly rules "never re-locks anything gated on mastery ... fog-of-war gates ... all keep reading the boolean unchanged" — so no extra "ever mastered" bookkeeping was added. `ProductMapScreen.tsx`'s old hardcoded-locked panel is replaced by a generic `FogGatePanel` (mockup's fog treatment: blurred lines + lock + a `next/link` unlock hint to `/learn/[skillId]`) wrapping two panes. Unlocked `cohort-curves` renders REAL per-sprint average happiness by customer **archetype** (the engine's only segment concept) as a multi-line chart on a shared 0-10 scale — sourced from a new optional `customerHappiness` field added to `metricsHistoryStore.ts`'s `MetricsSnapshot` (mirrors that store's own precedent: the engine has no per-customer history, so this slice starts *recording* it, same as W3-E did for the aggregate tiles; older snapshots honestly lack the field, never backfilled). New pure module `app/src/components/console/sim/cohortCurves.ts` documents exactly what this is and isn't ("not classic signup-cohort retention — this engine has no signup-date concept"). Unlocked `decision-annotations-history` lists every logged sprint rationale in one place (reuses the existing `deriveDecisionAnnotations` the metric-tile amber dots already compute; tapping a list row opens the same rationale sheet the dots do). Both panes show an honest empty-state caption (not fabricated data) when history/rationale is thin. 29 new tests (`fogOfWar.test.ts`: 10 pure gating/permanence, `cohortCurves.test.ts`: 12 pure derivation, `ProductMapScreen.test.tsx`: 6 locked-vs-unlocked render coverage, `metricsHistoryStore.test.ts`: 1 new case) — 883/883 total. All gates green: typecheck; lint at the 8-problem baseline (unchanged, none in touched files); build; both leak greps (`HIDDEN ROOT CAUSE`, `sk_test\|sk_live\|STRIPE_SECRET` in `.next/static/`) empty. Touched only `app/src/lib/fogOfWar.ts` (new), `app/src/components/console/sim/{cohortCurves.ts (new), ProductMapScreen.tsx}`, `app/src/store/metricsHistoryStore.ts` (additive field), and their tests — no engine/scenario/curriculum-content/learnStore-internals edits. Known follow-up (not this slice): `app/src/lib/skillUnlocks.ts` (W4-H, concurrently in flight) hand-maintains a temporary `SIM_UNLOCKS` copy of the old placeholder gate with its own note to merge from this registry once it existed — it now does; reconciling that map is W4-H's/a future slice's call, left untouched per the "don't touch shared src/lib files mid-flight" caution. |
| W4-H | Mastery decay (rusty + 90s refresh) + skill map as tech tree with in-sim unlock lines. | ✅ New pure module `app/src/lib/masteryDecay.ts`: additive strength model on top of boolean mastery, display-only by construction (no exported function can un-master anything). Curve: full strength (100) for a 14-day grace window, then linear decay to a 40 floor over the next 35 days, never lower — tabled in the file's header comment. Reads `lastPracticedAt` (new additive `SkillProgress` field in `learnStore.ts`, stamped on every `recordResult`/`recordPlacementPass` call); pre-existing persisted skills that predate the field fall back to `masteredAt` (already persisted), and a skill with neither renders at the floor rather than assuming freshness ("stale-unknown") — no migration function needed, purely additive/optional. `isRusty` flags mastered skills below a 70 threshold. Recovery: `learnStore.recordMaintenanceRep(skillId)` restamps `lastPracticedAt` to now (full reset), gated to a no-op for never-mastered skills; it deliberately does not touch `mastery`/`attempts`/streak. 90-SECOND REFRESH: new `app/src/lib/skillRefresh.ts` bridges the curriculum's 17-competency spine to the judgment deck's independent 8-bucket vocabulary (`COMPETENCY_TO_JUDGMENT`, a documented best-fit — no precise mapping exists since the two taxonomies are deliberately separate, per `curriculum/judgment/types.ts`), picks up to 2 judgment scenarios from that bucket, and picks the next industry in `INDUSTRIES` cyclically (deterministic, always different from the learner's current one) for the "different industry skin" ruling. Surfaced from a new node-detail sheet (`app/src/components/console/skills/SkillNodeSheet.tsx`) via a self-contained `RefreshFlow`/`RefreshCard` (a `--px-*`-styled sibling of `review/JudgmentCard.tsx`, not a reuse of it — that component is hard-coded to the older light-only `--color-*` tokens, see globals.css's own "SCOPE DECISION" comment, so reskinning it here would have risked a scope-creeping token migration; the DATA/scoring machinery, `resolveScenario`/`reviewStore.review`, is fully shared). Not wired into `/review` (judged non-trivial: that page has no per-skill entry point today). TECH TREE: new pure module `app/src/lib/skillTree.ts` maps `deriveSkillState`'s three reachable states (`mastered`/`active`/`locked` — `'available'` is declared but never returned) plus decay onto the mockup's four (`done`/`rusty`/`next`/`locked`), and flattens each level's skills into one ordered list (matches the mockup's level-grouped, not unit-grouped, `.tree`). `PracticeMap.tsx`'s `LevelSection` now renders `SkillTree`/`SkillNodeSheet` in place of the old per-unit `SkillCard` grid for any unlocked level; locked levels keep their existing collapsed locknote treatment unchanged. UNLOCK LINES sourced from `app/src/lib/skillUnlocks.ts`: real ("sim") lines are built straight from W4-G's concurrently-landed `fogOfWar.ts` `FOG_GATE_LIST` (composed fresh from `gate.label`, not string-surgery on `gate.unlockHint`, to keep the two surfaces' copy decoupled) — this reconciles W4-G's own "known follow-up" note about the old placeholder `SIM_UNLOCKS` map; every other skill gets a pedagogical fallback naming the next skill(s) sharing its competency (`Feeds: ...`) or, for the last skill on a thread, the competency it builds (`Builds ...`) — never a fabricated game power. SCHEDULER COHERENCE: `scheduler.ts`'s `competencyStrength`/`CompetencyCandidate`/`RankedCompetency` gained an additive, default-1 `decayFactor` parameter that discounts the coverage term before blending with sim evidence (all pre-existing call sites/tests unaffected at the default); `StandupView.tsx`'s Act-2 candidate build now passes each competency's `averageCompetencyDecay` (mastered-ready-skills only, 1 when none are mastered yet) so a fully-mastered-but-rusty competency resurfaces as a weakest-competency pick without touching gating. Verified live end-to-end in the browser (dev server): tech tree render, node sheet, a seeded-rusty node showing "40% · going stale", the full 2-card refresh flow in a different industry skin, and live strength recovering to 100% with mastery/scheduler position unaffected; dark theme confirmed via computed styles (`--px-*` resolves correctly). 92 new tests (`masteryDecay.test.ts`, `skillRefresh.test.ts`, `skillTree.test.ts`, `skillUnlocks.test.ts`, plus additive cases in `scheduler.test.ts`) — 883/883 total (includes W4-G's concurrently-landed suite). All gates green: typecheck; lint at the 8-problem baseline (unchanged, none in touched files); build; both leak greps (`sk_test\|sk_live\|STRIPE_SECRET`, `SUPABASE_SERVICE_ROLE_KEY` in `.next/static/`) empty. Touched only `app/src/lib/{masteryDecay,skillRefresh,skillTree,skillUnlocks}.ts` (new) + their tests, `app/src/lib/scheduler.ts` (+tests), `app/src/store/learnStore.ts` (additive), `app/src/components/console/skills/` (new: `SkillTree.tsx`, `SkillNodeSheet.tsx`, `RefreshCard.tsx`), `app/src/components/console/PracticeMap.tsx`, `app/src/components/console/standup/StandupView.tsx` — no engine/scenario/sim-directory/api-route edits; `fogOfWar.ts` and `ProductMapScreen.tsx` were read-only inputs (W4-G's files, landed concurrently, not edited here). |
| W4-I | Drill player card-stack + review deck (Leitner shelf, provenance chips) per mockups. | ✅ DRILL PLAYER: the 4 deterministic drill bodies (`ScoreRankLesson`/`ClassificationLesson`/`SizingLesson`/`SequencingLesson`) and their shared `LessonFrame` chrome are now `--px-*`-tokened (this is the drill loop's turn to opt into the Sim 2.0 dark-capable system — `LessonFrame.tsx` has exactly these 4 consumers, confirmed by grep, so the retheme is total; `FreeTextGradeLesson.tsx` (AI-graded drills) has its own independent chrome and is untouched). New `DrillCardStack.tsx` gives the two item-independent drill types — classification (Kano/MoSCoW) and sizing (T-shirt/estimation), where "is THIS one item right" is a real standalone question — the mockup's true one-interaction-per-screen pacing: one item card at a time (a peeking next-card behind it), picking an answer reveals THAT item's own verdict stamp (`stampIn` keyframe in `globals.css`, gated behind Tailwind's `motion-safe:` variant so `prefers-reduced-motion` renders instantly with no animation) + one-line "why" immediately. The relative-order drills (RICE/WSJF ranking, 5-Whys sequencing) keep their existing reorder-all-then-check interaction — forcing a false per-item verdict onto a relative-order task would misrepresent what "correct" means — restyled to the same token system with per-row stamp badges. New `BlockSummaryCard.tsx` (sourced by new pure `app/src/lib/blockSummary.ts`) replaces the old unconditional "Skill Mastered" `CompletionOverlay` for these 4: an honest tally (real correct/total from each drill's own grade call, threaded through via a new additive `DrillResult.tally` field), a competency-strength before→after read of `@/lib/masteryDecay`'s real decay model (before/after `SkillProgress` snapshotted around the `recordResult` call) — a delta is shown ONLY when the model actually moved, else the honest fallback ("lastPracticed restamped · strength holds at X%" or "X% to mastery") — and a "1 block closer" line sourced from `@/lib/skillUnlocks`'s `unlockLineFor` (the SAME registry the tech tree renders: a real fog-of-war gate's unlock text when the just-mastered skill is one of the two gates in `fogOfWar.ts`, else that module's own pedagogical "Feeds: ..." fallback), shown only the one moment a skill NEWLY crosses mastery this attempt — never fabricated, never shown for a re-practice of an already-mastered skill. REVIEW DECK: `JudgmentCard.tsx` restyled to `--px-*` (prop contract unchanged, so `/standup`'s warm-up — which embeds this same component inside its own `--px-*` page since W1-B — also stops rendering a light card on a dark page, a real pre-existing mismatch this incidentally fixes) with an additive optional `topSlot` for provenance chips. `ReviewView.tsx` fully restyled: new `LeitnerShelf.tsx` renders real per-box counts + interval labels from a new pure `reviewStore.boxShelf()` (a row per box even at zero, an honest "New" row for never-reviewed cards, due box(es) highlighted), shown on the "all caught up" deck-clear state alongside a real `learnStore` streak readout ("Streak safe · N days", worded as an ambient status echo, not a claim that review itself extends a nonexistent "review streak" — no such mechanic exists, so none was fabricated) and the existing real next-due date. Box-movement feedback ("Box N → Box N+1 · next seen in D days") is computed by a new pure `reviewStore.describeBoxMove()` fed by the SAME `scheduleNext()` the store's `review()` action itself calls, so the line can never drift from what actually gets persisted. PROVENANCE: `reviewStore.CardSchedule` gains one additive optional field, `resurfaced?: boolean` — set by `resurface()` (called today only from the off-limits `src/components/console/sim/{InboxTurn,EventStep,SimEndPanel}.tsx`, read-only inputs to this slice) on any card it pulls forward, and naturally cleared on that card's next review since `scheduleNext()` always builds a fresh schedule object (old persisted schedules simply lack the field; no migration needed). New pure `app/src/lib/reviewProvenance.ts`'s `deriveProvenance()` reads that flag to decide a due card's RENDERED industry — home industry normally, or the next industry in `skillRefresh.ts`'s existing cyclic `pickRefreshIndustry()` rotation (reused, not duplicated) when resurfaced — and reports whether each chip is warranted: "⟲ From your run" (`fromRun`, no scenario/sprint detail — the sim call sites only ever pass card ids to `resurface()`, never a run/sprint context, and those call sites are off-limits to edit this slice, so the chip stays honestly generic rather than fabricating provenance detail that isn't recorded) and "Same skill · <industry> skin" (`showSkinChip`, whenever the rendered industry differs from home — true today exactly when a card was resurfaced, computed independently so a future second variation source needs no change here). The review SESSION QUEUE snapshots each due card's `resurfaced` flag once at hydration (same pattern as the existing due-id snapshot) so a chip never flips mid-session. Verified live in the browser (dev server), both themes: card-stack stamps/peek/explanation, block-summary honest tally+strength+closer line, and a forced-resurfaced card rendering in a different (Fintech) industry skin with both chips — screenshots not committed. 21 new tests (`reviewStore.test.ts` +5 for `boxShelf`/`describeBoxMove`, `reviewProvenance.test.ts` 4, `blockSummary.test.ts` 6, plus existing suite unaffected since no render tests existed for the touched lesson/review components before this slice) — 898/898 total, all pre-existing behavioral tests (answer flows, Leitner scheduling math) green and untouched in logic. All gates green: typecheck; lint at the 8-problem baseline (the one hit in `ReviewView.tsx` — `react-hooks/set-state-in-effect` on the pre-existing due-queue snapshot effect — predates this slice, confirmed via `git show HEAD`, same line/structure before my edit); build; both leak greps (`HIDDEN ROOT CAUSE`, `sk_test\|sk_live\|STRIPE_SECRET` in `.next/static/`) empty. Touched only `app/src/components/console/lesson/{LessonFrame,ScoreRankLesson,ClassificationLesson,SizingLesson,SequencingLesson}.tsx`, new `{DrillCardStack,BlockSummaryCard}.tsx`, `app/src/components/console/review/{JudgmentCard,ReviewView}.tsx`, new `LeitnerShelf.tsx`, `app/src/store/reviewStore.ts` (additive), new `app/src/lib/{blockSummary,reviewProvenance}.ts`, `app/src/app/globals.css` (additive keyframe), and their tests — no engine/scenario/api-route/frozen-file edits; `src/components/console/sim/*` was read-only. |
| W5-J | Multi-party QBR AI route (guardrail stack, degrade) + in-sim graded-artifact moments modulating engine risk. | ✅ QBR ROUTE: new `app/src/app/api/qbr/route.ts` clones interview-ammo's guardrail stack byte-for-byte in the same order (rate limit `{8, 10min}` → parse/validate → identity + `reconcileBudgetOnLapse` → no-key calm 200 → global ceiling → per-user reserve immediately before spend → settle actual → settle-to-zero in catch → `logUsage`). ONE Haiku call (`MAX_TOKENS` 1400) orchestrates the THREE roster people the client sends (exec chair/eng lead/sales-cs, from `state.people`) discussing the season — coherent disagreement grounded strictly in a submitted `season` record (confidence, expectations, 5-dim score, ≤20 bounded/clamped decision-log-style `sprintFacts`, ~16k char cap) — plus a closing committee line; server-side normalization clamps every `speakerId` to the submitted roster ids (an invented speaker's turn is dropped, never trusted through), caps turns at 12, clamps every text field, and returns the sibling `{meeting, raw}` / `{raw, meeting: null}` / calm-`unavailable` shapes. Wired into `SeasonScreen.tsx`'s QBR card (replacing the `TODO(W5-J)` marker): a "Convene the QBR" button (`useQBR.ts` hook, `authHeaders()` spread) renders the meeting as speaker-chip chat turns + the closing line, persisted additively into `decisionLogStore`'s new `qbrMeetings` (one per run, mirrors `interviewStories`'s persistence pattern) so a convened meeting survives navigation without re-spending a call; calm-unavailable/error render inline, score/verdict above are never recomputed by any of this (design doc §4: AI is garnish). New pure `sim/qbr.ts` shapes the request (`buildQBRRoster` picks exactly the 3 documented roles in order; `buildQBRSprintFacts` bounds/projects the decision log; `buildQBRSeasonInput` assembles the payload) so SeasonScreen never hand-rolls the shape. IN-SIM ARTIFACT MOMENT: when a sprint's plan has a placed release card (`state.releaseCardPosition !== null`), InboxTurn's commit sheet offers an optional "Write the launch PRD" button that opens `ReleasePrepSheet.tsx` — a compact `--px-*`-styled sheet (reuses the exported `Sheet` chrome from `InboxTurn.tsx`, not a light-token component per W4-H's own precedent) that reuses the EXISTING v2 grading machinery verbatim (`onePagePrd` artifact content, `resolveArtifact`/`composeSubmission`/`isSubmittable`, the unmodified `useArtifactGradeV2` hook against the unmodified `/api/grade-artifact` v2 route). The returned `overallScore` is banded (new pure `engine/releasePrep.ts`: ≥80 strong / ≥60 mixed / else weak — server-independent, computed client-side) and immediately dispatched as a new additive engine action, `set-release-prep { quality }`, gated to the `planning` phase (before commit, so it lands before `execute-iteration` reads `tech` for the capacity roll). EFFECT TABLE (`RELEASE_PREP_EFFECTS`, one-time, bounded, on the existing `tech.capacityVariance`/`tech.techDebt` knobs only — no new outcome math): strong → capacityVariance −1 / techDebt +0; mixed → +0 / +0 (a true no-op); weak → capacityVariance +1 / techDebt +2 (clamped to [1,8] and [0,100] respectively). Skipping the moment, or never placing a release card, leaves the sprint byte-for-byte identical to today — no new `GameState` field, no migration. The score also lands as an additive optional `artifactGrade?: number` on this sprint's `decisionLogStore` entry (Career File record only; the engine effect is independent and does not read it back). TESTS: 18 new `/api/qbr` route tests (reconcile-called, 401, no-key/at-capacity/allowance calm-200, settle-to-zero on failure, speaker-id clamping, turn cap, empty-text drop, roster-cap defensive bound); 23 new engine tests (`releasePrep.test.ts`: score bands, the exact effect table, clamping at both bounds, determinism, no-mutation, phase-gating, no-op-when-never-dispatched, old-save shape unaffected); 8 new pure-logic tests (`sim/qbr.test.ts`: roster role-picking/omission, sprint-fact bounding/projection, score-dims passthrough); 2 new `InboxTurn.test.tsx` render tests (button absent with no release card; present + skip-returns-to-commit + commits with `artifactGrade` undefined when skipped) — 967/967 total. All gates green: typecheck; full suite 967/967; lint at the exact 8-problem baseline (3 new route-file unused-interface warnings were introduced then removed before the final run — the shipped diff adds zero new lint problems); build; both leak greps (`HIDDEN ROOT CAUSE`, `sk_test\|sk_live\|STRIPE_SECRET` in `.next/static/`) empty. Touched: new `app/src/app/api/qbr/{route.ts,__tests__/route.test.ts}`, new `app/src/engine/releasePrep.ts` + `__tests__/releasePrep.test.ts`, `app/src/engine/{types.ts,step.ts,index.ts}` (additive), new `app/src/components/console/sim/{useQBR.ts,qbr.ts,__tests__/qbr.test.ts,ReleasePrepSheet.tsx}`, `app/src/components/console/sim/{SeasonScreen.tsx,InboxTurn.tsx,__tests__/InboxTurn.test.tsx}` (additive; `Sheet` given an `export`), `app/src/store/decisionLogStore.ts` + its test (additive `qbrMeetings`/`artifactGrade`) — no existing API route touched, no frozen file touched, no `src/lib/telemetry`/learn/review edits. |
| W5-K | Shared learner model v0: emit `exercise_events` when signed in (RLS append-own already allows it). | ✅ New `app/src/lib/telemetry/exerciseEvents.ts`: `emitExerciseEvent({kind, competency?, skillId?, score?, payload?})` — fire-and-forget, silent no-op on SSR/unconfigured/signed-out (checked via `useAuthStore`, the same signed-in signal `@/lib/sync/engine.ts`'s `schedulePush` already uses), never throws (the whole flush body — including `browserClient()`/`useAuthStore.getState()` themselves — is one try/catch, not just the `insert` call, since it runs detached inside a `setTimeout`). Batching mirrors `sync/engine.ts`'s `schedulePush`/`flushPush` pair exactly: a 2s trailing-edge debounce (queue, don't insert; each new event re-arms the timer) collapses a burst (e.g. a whole drill block) into one `insert` call. Payload is a hard privacy rule documented in the file header: ids/scores/counts/booleans only, NEVER graded text (no artifact submissions, no interview transcripts, no free-text drill answers). Instrumented 4 non-sim completion points: (1) `LessonFrame.tsx` `handleContinue` — kind `'drill'`, covers all 4 deterministic drill bodies (ScoreRank/Classification/Sizing/Sequencing → rice/cost-of-delay/kano-moscow/estimation/problem-framing), score = the same normalized value already passed to `recordResult` (correct ? 1 : tally fraction), payload `{skillId, correct, tally}`; (2) `ValueVsEffortLesson.tsx` + `FreeTextGradeLesson.tsx` (the two drill loops outside `LessonFrame`'s shared chrome — the exemplar and the 4 AI-graded free-text drills: jtbd/user-interviews/pre-mortem/pr-faq) — same kind `'drill'`, one-line calls at their own `handleContinue`, so every skill reachable through `LessonRouter`'s switch now emits; (3) `ReviewView.tsx` `handlePrimary` — kind `'judgment_card'`, competency = the judgment deck's own local vocabulary (`JudgmentScenario.competency`, deliberately not the curriculum spine), score 0/1, payload `{scenarioId, box: <post-review Leitner box>, resurfaced}`; (4) `ArtifactLesson.tsx` `handleSubmit` — kind `'artifact'`, fires only when a real V2 verdict comes back (never on unavailable/error), score = `overallScore/100`, payload `{skillId, version, passed}`; (5) `InterviewSession.tsx` `handleEnd` — kind `'interview'`, score = `overallScore/100`, payload `{caseId, recommendation}`, no skillId/competency (interview cases are deliberately not skill-scoped, per `curriculum/interview/types.ts`). `roleplay` and `sim_decision` are NOT wired this slice: roleplay wasn't in this slice's instrumentation list, and `sim_decision` (commit-time in `InboxTurn.tsx`/`decisionLogStore`) is explicitly the other builder's file — the emitter is ready for that call site the moment it lands. 14 new tests (`exerciseEvents.test.ts`: degrade behavior incl. an unhandled-rejection regression test for a synchronously-throwing `browserClient()` at flush time, batching/debounce-collapse, and a payload-privacy-shape pin per instrumented kind) — 930/930 total, zero regressions in the 6 touched files' existing suites. Gates: typecheck clean in every file this slice touched (isolated `tsc --noEmit` run confirms both current repo-wide errors live in `src/engine/__tests__/releasePrep.test.ts`, the concurrently in-flight W5-J builder's own uncommitted file, off-limits here); full suite 930/930; lint isolated to this slice's files shows only the one pre-existing `ReviewView.tsx` `react-hooks/set-state-in-effect` baseline error (documented in W4-I's row above, unrelated to this slice's edits) and nothing new; build green; both leak greps (`HIDDEN ROOT CAUSE`, `sk_test\|sk_live\|STRIPE_SECRET` in `.next/static/`) empty. Touched only `app/src/lib/telemetry/exerciseEvents.ts` (new) + its test, and one-line call sites in `app/src/components/console/lesson/{LessonFrame,ValueVsEffortLesson,FreeTextGradeLesson,ArtifactLesson}.tsx`, `app/src/components/console/review/ReviewView.tsx`, `app/src/components/interview/InterviewSession.tsx` — no engine/scenario/api-route/sim-directory/sync-allowlist edits. |

| QA-1 | In-browser QA sweep of the merged whole + fix slice. | ✅ Full end-to-end browser QA (mobile viewport, both themes) of /play (fresh run played through commit→event→cliffhanger→review, plus forced fired→offers and complete→QBR flows), /standup, /review, /report, home tech tree incl. a forced-rusty 90s refresh. Findings fixed in this slice: **(P0)** /standup crashed on load — `masteredIds()` called inside a zustand selector built a fresh Set per read (`getServerSnapshot should be cached` loop); now derived via useMemo from the stable `progress` ref. **(P1)** dark mode: SimTabs never painted `--px-ground` so the light body bled through every tab (QBR gradient washed out); PlanStep/OutcomeStep/DebriefStep/EventBeats (+ StepHeader/SimDock/Coachmark chrome) were still light-token with near-black-on-dark headings — all rethemed to `--px-*`, zero behavior change, both themes browser-verified. **(P2)** RecapCard rendered the just-resolved sprint's outcome above the cliffhanger during review — now planning-only; all four AI routes leaked raw provider JSON on a rejected key — new `lib/providerAuthError.ts` duck-types 401/403 in each catch (after settle-to-zero, order untouched) and returns the calm `unavailable` shape, one pinned test per route; Career File empty state linked to nonexistent `/play` — now the ladder's first rung; QBR convene on a zero-entry run 400'd with a raw validation string — now gated with a calm line. **(nits)** `deriveOutcomeSummary` no longer counts the release card as "slipped"; FiredBeat/unavailable-copy dedupes; grade-artifact no-key message made surface-neutral. Gates: typecheck; 972/972 (5 new tests); lint at the 8-problem baseline; build; both leak greps empty. NOT covered by the sweep: drill card-stack/BlockSummary interiors, desktop-width pass, live AI/auth/checkout (Mike-gated). Full QA + builder reports in the session scratchpad. |

## Standing build rules (every slice)
- Sonnet builders, leading no-delegation rule, reports to scratch files; Fable line-reviews AI/money surfaces and the engine's state-migration points.
- Frozen: `artifactGrader.ts`, `stripe.ts`, `entitlements.ts`, `budget.ts`, existing route guardrail order. New AI routes clone the interview route's stack in the same order.
- Engine stays a pure seeded reducer; additive optional state only (old persisted `GameState` snapshots must keep loading). All AI is garnish — outcomes engine-computed.
- Gates per slice: typecheck · full test suite green · lint = the 8 pre-existing baseline problems only · (UI slices) build + both bundle-leak greps empty.
- Old sim presentation (6-step `SimRunner`) is REPLACED by W2-D/W3 — engine and its tests untouched; component tests adapted, not deleted silently.

## Decisions this engagement
- No feature flag for the sim rebuild: pre-launch, no sim users; the wedge surfaces (`/interview`, `/artifact`, checkout) are untouched by every slice.
- Theme default = system preference; explicit toggle persists (`data-theme` on root); game surfaces use the same tokens as the rest of the app.
- Cred/XP economy deferred (see scope ruling).

## Open questions carried from design doc §7
Career File naming · board-confidence tuning · firing UX severity · rationale-capture friction · Career File export server-side vs browser-side · (new) Cred economy timing.

## Session log
- 2026-07-12: engagement started. Branch `claude/sim2-wave1` from `acd6f24`. W1-A + W1-B launched.
- 2026-07-12 (later): W1-A/B, W2-C/D, W3-E/F all landed on this branch — the Sim 2.0 core (people, board, inbox, map, season) + theme layer + /standup. 803/803 tests. W4-G/H/I + W5-J/K remain.
- 2026-07-13: first in-browser QA of the merged whole (post-#12) + the QA-1 fix slice (branch `claude/sim-2-phase-2-build-0a2891`): /standup P0 crash, sim dark-mode ground + light-token retheme, cliffhanger spoiler, provider-auth calm degrade, and the P2/nit set. 972/972 tests.
